# Dirb 目录扫描

Dirb 是一款 Web 内容扫描器，用于查找隐藏的 Web 对象（如目录、文件等）。

## 功能入口

在 Termux-X 渗透测试菜单中点击 **Dirb** 图标。

## 功能说明

Termux-X 提供了 Dirb 的快捷操作界面 (`DirbDialog`)：
- **目标 URL**：输入要扫描的网站地址。
- **字典选择**：选择用于扫描的字典文件。
- **扫描启动**：一键启动目录爆破。

该工具将在 Kali 环境中运行。
