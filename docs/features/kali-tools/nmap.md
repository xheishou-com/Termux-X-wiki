# Nmap 网络扫描

Nmap (Network Mapper) 是一款用于网络发现和安全审计的网络安全工具。

## 功能入口

在 Termux-X 渗透测试菜单中点击 **Nmap** 图标。

## 功能说明

Termux-X 提供了 Nmap 的快捷启动和参数配置界面 (`NmapDialog`)，您可以：
- **目标扫描**：输入目标 IP 或域名。
- **端口扫描**：指定扫描端口范围。
- **高级选项**：配置扫描类型（如 SYN 扫描、UDP 扫描等）。

启动后，Termux-X 将在 Kali NetHunter 环境中执行 nmap 命令并显示结果。
