# Sqlmap 注入工具

Sqlmap 是一款自动化的 SQL 注入和数据库接管工具。

## 功能入口

在 Termux-X 渗透测试菜单中点击 **Sqlmap** 图标。

## 功能说明

通过 Termux-X 的 `SqlmapDialog`，您可以：
- **URL 注入**：输入目标 URL 进行检测。
- **参数配置**：设置 `--dbs`, `--tables`, `--dump` 等常用参数。
- **POST 注入**：支持粘贴数据包进行 POST 注入测试。
- **Tamper 脚本**：选择绕过脚本。
