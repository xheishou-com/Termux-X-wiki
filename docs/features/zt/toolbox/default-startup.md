# 默认启动文件 (Default Startup File)

**功能简介**：
该功能用于一键恢复 Termux 的默认 Shell 启动配置文件（通常是 `.bashrc` 或 `init.sh`）。

**适用场景**：
- 当您错误地修改了启动脚本（例如添加了错误的命令或环境变量），导致终端打开即闪退或无法正常输入命令时。
- 想要清除所有自定义的 Shell 配置，恢复到刚安装时的纯净状态。

**使用方法**：
1. 双击终端屏幕打开工具箱。
2. 点击 **默认启动文件** 按钮。
3. 系统会提示确认覆盖，确认后将重置启动文件。
4. 重启终端（或新建会话）即可生效。
